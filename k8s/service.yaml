apiVersion: v1
kind: Service

metadata:
  name: harness-python-service

spec:
  type: NodePort

  selector:
    app: harness-python-app

  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
